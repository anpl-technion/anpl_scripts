# Installation steps for new user (not the first user on the machine)

# before clone anpl_scripts repo
# cd
# mkdir ANPL && cd ANPL
# git clone https://bitbucket.org/ANPL/anpl_scripts scripts

# after scripts repo is clone
cd
cd ANPL/scripts/ubuntu-16-04
./setup-ubuntu-env.sh

# close terminal and open a new one
cd ANPL/scripts/ubuntu-16-04
./setup-scripts-env-ubuntu.sh

# close terminal and open a new one
set-user-bashrc.sh
